services:
  - type: web
    name: resume-analyzer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./setup.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
